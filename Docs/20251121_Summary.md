# PathcraftAI 개발 일지 - 2025-11-21

## 오늘의 주요 작업

### 1. 3.27 한국어 번역 시스템 구현

POE 3.27 Keepers of the Flames 업데이트에 맞춰 한국어 번역 데이터를 수집하고 통합하는 시스템을 구축했습니다.

#### 수집된 데이터 통계
| 소스 | 아이템 | 스탯 | 기타 |
|------|--------|------|------|
| PoeCharm CSV | 2,894 | - | 스킬 1,102 |
| Awakened POE Trade | 4,716 | 6,499 | - |
| POE Trade API | 1,418 | 14,890 | 정적 1,912 |
| **병합 합계** | **5,847** | **21,389** | **스킬 1,102** |

#### 생성된 파일
- `src/PathcraftAI.Parser/awakened_translation_extractor.py` - Awakened POE Trade 한국어 데이터 추출기
- `src/PathcraftAI.Parser/poe_trade_korean_fetcher.py` - POE Trade API 한국어 데이터 수집기
- `src/PathcraftAI.Parser/data/merged_translations.json` - 병합된 번역 데이터
- `src/PathcraftAI.Parser/data/awakened_translations.json` - Awakened 데이터
- `src/PathcraftAI.Parser/data/poe_trade_korean.json` - Trade API 데이터

### 2. LibGGPK3 통합

POE Bundle 파일에서 직접 데이터를 추출할 수 있도록 LibGGPK3를 프로젝트에 통합했습니다.

#### 설정 과정
1. GitHub에서 LibGGPK3 클론: `tools/LibGGPK3`
2. `PathcraftAI.Core.csproj`에 프로젝트 참조 추가
3. `GameDataExtractor.cs` 서비스 구현

#### GameDataExtractor 기능
- POE Content.ggpk에서 Bundle 파일 추출
- 한국어 데이터 파일 추출 (Data/Korean/)
- 패시브 트리, 젬 데이터 접근 가능

### 3. 번역 매핑 시스템 개선

`auto_recommendation_engine.py`의 `load_korean_translations()` 함수를 업데이트해서 병합된 번역 파일을 우선 로드하도록 변경했습니다.

우선순위:
1. `merged_translations.json` (병합된 최신 데이터)
2. `poe_translations.json` (PoeCharm 데이터)

## 기술적 결정 사항

### PyPoE/RePoE 대신 다른 방법 사용
- PyPoE: 3년 이상 업데이트 안됨, POE 3.21+ Bundle 형식 미지원
- RePoE: 업데이트 중단
- **결론**: POE Trade API + Awakened POE Trade + LibGGPK3 조합 사용

### POE 데이터 추출 방법 조사
- **공식 API**: POE Trade API가 `lang=ko` 파라미터로 한국어 공식 지원
- **Awakened POE Trade**: 이미 한국어 번역 데이터 포함 (items.ndjson, stats.ndjson)
- **LibGGPK3**: C#/.NET 네이티브로 Bundle 파일 직접 추출 가능

### 4. 자동 업데이트 파이프라인 구축

POE 패치 감지 및 번역 데이터 자동 갱신 시스템을 구현했습니다.

#### 생성된 파일
- `src/PathcraftAI.Parser/translation_update_pipeline.py` - 자동 업데이트 파이프라인

#### 기능
- POE 패치 버전 확인 (현재 리그 기반: "Keepers")
- 24시간 캐시 시스템
- Awakened + Trade API + PoeCharm 데이터 자동 병합
- 상태 조회 기능

#### 사용법
```bash
# 상태 확인
python translation_update_pipeline.py --status

# 업데이트 실행 (캐시 확인 후)
python translation_update_pipeline.py

# 강제 업데이트
python translation_update_pipeline.py --force
```

### 5. DAT64 파싱 시스템 구현

POE DAT64 바이너리 파일을 파싱하여 한국어 번역 데이터를 직접 추출할 수 있는 시스템을 구현했습니다.

#### 생성된 파일
- `src/PathcraftAI.Core/Dat64Parser.cs` - DAT64 파일 파서 코어
- `src/PathcraftAI.Core/KoreanDatExtractor.cs` - 한국어 DAT 데이터 추출기
- `tools/DatExtractorTest/` - DAT 추출 테스트 프로젝트

#### DAT64 파일 구조
- 헤더: 4바이트 (행 수)
- 고정 데이터 섹션: 행별 고정 크기 데이터
- 마커: 8바이트 (0xBBBBBBBBBBBBBBBB)
- 변수 데이터 섹션: UTF-16LE 문자열 등

#### 지원 DAT 파일
- BaseItemTypes.dat64 - 기본 아이템
- ActiveSkills.dat64 - 액티브 스킬
- SkillGems.dat64 - 스킬 젬
- Stats.dat64 - 스탯
- PassiveSkills.dat64 - 패시브 스킬

#### 요구사항
- Oodle DLL (oo2core_*.dll) - POE 폴더에서 복사 필요

### 6. Trade URL 자동 생성 기능 구현

업그레이드 추천 아이템에 대한 POE Trade URL을 자동으로 생성하는 기능을 구현했습니다.

#### 수정된 파일
- `src/PathcraftAI.Parser/poe_trade_api.py` - Trade URL 생성 메서드 추가
- `src/PathcraftAI.Parser/analyze_user_build.py` - 업그레이드 추천에 Trade URL 포함

#### 새로운 API 메서드
```python
# 유니크 아이템 검색 URL 생성
api.search_unique_item_url("Mageblood")

# 스탯 기반 아이템 검색 URL (모드 타입 지정 가능)
api.search_stat_item_url(
    item_type="Ring",
    stat_requirements=[
        {"id": "explicit.stat_123", "min": 50},  # 명시 모드
        {"id": "implicit.stat_456", "min": 10},  # 암시 모드
        {"id": "pseudo.pseudo_total_elemental_resistance", "min": 60}
    ],
    max_price=30
)

# 특정 모드를 가진 아이템 검색 URL
api.search_item_with_mod_url(
    item_type="Helmet",
    mods=[
        {"id": "explicit.stat_1050105434", "min": 80},  # +# to maximum Life
        {"id": "crafted.stat_3299347043", "min": 20},   # 제작 저항
        {"id": "fractured.stat_789"},                    # 프랙처 모드
    ],
    max_price=100,
    ilvl_min=85
)

# 젬 검색 URL (이상 젬 포함)
api.search_gem_url(
    gem_name="Vaal Grace",
    min_level=21,
    min_quality=23,
    alternate_type=1  # 1=Anomalous, 2=Divergent, 3=Phantasmal
)

# 높은 pDPS 무기 검색 URL
api.search_high_pdps_weapon_url(
    min_pdps=500,
    weapon_type="One Hand Sword",
    max_price=200
)
```

#### 지원하는 모드 타입
- `explicit`: 명시 모드
- `implicit`: 암시 모드
- `crafted`: 제작 모드
- `enchant`: 인챈트
- `pseudo`: 가상 스탯 (총 저항, 총 생명력 등)
- `fractured`: 프랙처 모드
- `veiled`: 베일 모드

#### 추가 필터
- 아이템 레벨 (`ilvl_min`, `ilvl_max`)
- 퀄리티 (`quality_min`)
- 방어구 스탯 (`armour_min`, `evasion_min`, `energy_shield_min`)
- 무기 스탯 (`dps_min`, `pdps_min`, `edps_min`, `crit_min`, `aps_min`)
- 젬 필터 (`gem_level_min`, `gem_quality_min`, `gem_alternate`)
- 희귀도 (`rarity`: normal/magic/rare/unique)

#### 업그레이드 추천 출력 형식
```json
{
    "item_name": "Mageblood",
    "reason": "Recommended upgrade for Death's Oath",
    "chaos_value": 23350,
    "trade_url": "https://www.pathofexile.com/trade/search/Keepers/XXXXX"
}
```

### 7. 한국어 스탯 매핑 시스템 구현

한국어 스탯 텍스트를 POE Trade API stat ID로 자동 변환하는 시스템을 구현했습니다.

#### 생성된 파일
- `src/PathcraftAI.Parser/korean_stat_mapper.py` - 한국어 스탯 매퍼
- `src/PathcraftAI.Parser/test_korean_stat_search.py` - 테스트 스크립트

#### 주요 기능
- Awakened POE Trade stats.ndjson (9,875개 한국어 스탯) 기반
- 한국어 텍스트 → Trade API stat ID 변환
- Pseudo stat 단축키 지원 (life, ele_res, move_speed 등)
- Fuzzy 매칭으로 유사한 스탯 검색

#### 새로운 API 메서드
```python
# 한국어 스탯으로 검색
api.search_korean_stats_url(
    item_type="Ring",
    korean_stats=[
        {"text": "총 생명력", "min": 70},
        {"text": "총 원소 저항", "min": 80}
    ],
    max_price=50
)

# 단축키로 빠른 검색
api.search_with_shortcuts_url(
    item_type="Boots",
    shortcuts={
        "life": {"min": 60},
        "move_speed": {"min": 25},
        "ele_res": {"min": 60}
    },
    max_price=100
)
```

#### 사용 가능한 단축키
| 단축키 | 스탯 |
|--------|------|
| life | 총 생명력 |
| mana | 총 마나 |
| es | 총 에너지 실드 |
| ele_res | 총 원소 저항 |
| chaos_res | 총 카오스 저항 |
| str/dex/int | 총 힘/민첩/지능 |
| attack_speed | 총 공격 속도 |
| cast_speed | 총 시전 속도 |
| crit_chance | 전역 치명타 확률 |
| crit_multi | 전역 치명타 배율 |
| phys_dmg | 물리 피해 증가 |
| ele_dmg | 원소 피해 증가 |
| spell_dmg | 주문 피해 증가 |
| move_speed | 이동 속도 |
| gem_level | 젬 레벨 |

#### 아이템 카테고리 매핑
- 방어구: Boots, Helmet, Gloves, Body Armour, Shield
- 악세서리: Ring, Amulet, Belt
- 무기: One Hand Sword, Two Hand Sword, Bow, Staff, Claw, Dagger, Wand, Sceptre 등
- 주얼: Jewel, Abyss Jewel, Cluster Jewel
- 플라스크: Flask, Life Flask, Mana Flask, Utility Flask

### 8. UI Trade URL 클릭 처리 구현

업그레이드 추천 아이템에 Trade URL 클릭 버튼을 추가했습니다.

#### 변경된 파일
- `src/PathcraftAI.UI/MainWindow.xaml` - Trade 버튼 UI 추가
- `src/PathcraftAI.UI/MainWindow.xaml.cs` - OpenTradeUrl_Click 핸들러

#### 주요 기능
- UpgradeSuggestion 클래스에 TradeUrl 속성 추가
- 업그레이드 아이템 옆에 "🔍 Trade" 버튼 표시
- 클릭 시 기본 브라우저에서 Trade URL 열기
- TradeUrl이 있는 경우에만 버튼 표시

### 9. Gemini LLM 지원 확인

build_guide_generator.py에 Gemini 지원이 이미 완전히 구현되어 있음을 확인했습니다.

#### 사용 방법
```bash
python build_guide_generator.py --keyword "Death's Oath" --llm gemini --api-key YOUR_KEY
```

#### 특징
- Google AI Studio에서 무료 API 키 발급: https://makersuite.google.com/
- 일일 60회 요청 무료
- Free tier 사용자에게 권장

### 10. 검색 필터 UI 구현

Browse Builds 탭에 클래스, 정렬, 예산 필터를 추가했습니다.

#### 추가된 필터
- **클래스 필터**: All Classes, Witch, Shadow, Ranger, Duelist, Marauder, Templar, Scion
- **정렬 필터**: 조회수 높은순, 최신순, 좋아요순, 가격 낮은순
- **예산 필터**: 전체, ~50c, ~100c, ~500c, ~1000c, 1000c+

#### 변경된 파일
- `src/PathcraftAI.UI/MainWindow.xaml` - 필터 ComboBox UI 추가
- `src/PathcraftAI.UI/MainWindow.xaml.cs` - 필터 이벤트 핸들러 추가

### 11. POB 아이템 모드 파싱 개선

POB에서 가져온 아이템의 모드(Iron Will, corrupted implicits 등)를 정확하게 파싱하도록 개선했습니다.

#### 수정된 파일
- `src/PathcraftAI.Parser/pob_parser.py` - 아이템 모드 파싱 로직 개선

#### 주요 개선사항
- `{mutated}`, `{crafted}`, `{fractured}` 등 태그 처리
- Iron Will, Corrupted implicits 인식
- 모드 값에서 태그 제거 후 정규화

### 12. poe.ninja 캐싱 시스템 구현

poe.ninja API 타임아웃 문제를 해결하기 위해 파일 기반 캐싱 시스템을 구현했습니다.

#### 수정된 파일
- `src/PathcraftAI.Parser/poe_ninja_api.py` - 캐싱 시스템 추가
- `src/PathcraftAI.Parser/build_guide_system.py` - 캐시된 가격 사용

#### PriceCache 클래스
```python
class PriceCache:
    def __init__(self, cache_dir: str = None, ttl_seconds: int = 3600):
        # 파일 기반 캐싱 (TTL 1시간)
        self.cache_dir = Path(cache_dir)
        self.ttl_seconds = ttl_seconds
        self._lock = threading.Lock()  # 스레드 안전
```

#### POENinjaAPI 개선사항
- `use_cache` 및 `cache_ttl` 파라미터 추가
- `preload_cache(background=True)` - 백그라운드 캐시 로딩
- `get_all_unique_prices()` - Chaos 단위 전체 가격 조회
- `get_item_price(item_name)` - 단일 아이템 가격 조회
- 메모리 캐시로 2차 성능 최적화

#### 성능 개선
```
첫 번째 조회: 1379 아이템 (1.11초)
두 번째 조회: 2748 아이템 (0.46초, 캐시)
```

#### 가격 조회 예시
```
Nebulis: 22c
The Surrender: 402c (3.0d)
Aegis Aurora: 19c
Mageblood: 28316c (211.0d)
```

#### 해결된 문제
- **Before**: `HTTPSConnectionPool(host='poe.ninja', port=443): Read timed out.`
- **After**: 캐시 기반으로 안정적인 가격 조회

## 남은 작업

1. **DAT64 실제 테스트** - POE 설치 환경에서 Oodle DLL과 함께 테스트
2. **필터 로직 연동** - Python 백엔드에 필터 파라미터 전달
3. **Trade 검색 개선** - 한국어 스탯 기반 검색 고도화
4. **업그레이드 가이드 Trade 연동** - 추천 아이템 Trade URL 자동 생성

## Git 커밋

### 커밋 d860ef1
- POB 아이템 모드 파싱 개선
- poe.ninja 캐싱 시스템 구현
- build_guide_system 가격 조회 통합

## 빌드 상태

- ✅ PathcraftAI.Core 빌드 성공 (LibGGPK3 포함)
- ✅ Python 번역 추출기 정상 작동
- ✅ 병합된 번역 데이터 생성 완료
- ✅ poe.ninja 캐싱 시스템 정상 작동

## 참고 사항

- LibGGPK3는 NuGet에 없어서 GitHub에서 직접 클론 후 프로젝트 참조로 추가
- POE Trade API는 Rate limiting 준수 필요 (5-6 요청/초)
- Awakened POE Trade 데이터는 `tools/awakened-poe-trade/renderer/public/data/ko/`에 위치
